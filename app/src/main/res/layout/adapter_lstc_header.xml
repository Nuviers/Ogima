<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/linearLayoutLstcHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/estilo_background_inicio"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/_8sdp"
            android:background="@drawable/background_caixa_texto"
            android:orientation="horizontal"
            android:padding="@dimen/_8sdp">

            <ImageButton
                android:id="@+id/imgBtnCadComunidade"
                app:srcCompat="@drawable/icon_chat_black"
                app:tint="#FFFFFF"
                android:layout_width="@dimen/_40sdp"
                android:layout_height="@dimen/_40sdp"
                android:layout_margin="@dimen/_8sdp"
                android:background="@drawable/round_imagebackground"
                android:scaleType="fitCenter" />

            <Button
                android:id="@+id/btnCadComunidade"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/_8sdp"
                android:background="@drawable/background_caixa_texto"
                android:text="Criar Comunidade"
                android:textAllCaps="false"
                android:textColor="#FFFFFF"
                android:textSize="@dimen/_15ssp" />
        </LinearLayout>
    </LinearLayout>
</LinearLayout>